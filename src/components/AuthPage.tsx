import{useState,type FormEvent}from'react';import{LockKeyhole,Mail,User}from'lucide-react';import{supabase}from'../lib/supabase';import'./auth.css';
export function AuthPage(){const[signup,setSignup]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage('');const form=new FormData(e.currentTarget),email=String(form.get('email')),password=String(form.get('password')),full_name=String(form.get('name')||'');const{error}=signup?await supabase.auth.signUp({email,password,options:{data:{full_name},emailRedirectTo:location.origin+location.pathname}}):await supabase.auth.signInWithPassword({email,password});setBusy(false);setMessage(error?error.message:signup?'Đăng ký thành công. Hãy kiểm tra email xác minh.':'Đăng nhập thành công.');}
return <main className="auth-page"><section className="auth-brand"><img src={`${import.meta.env.BASE_URL}favicon.svg`} /><h1>LAC HONG UNIVERSITY</h1><p>Hệ thống quản lý hồ sơ và văn bản</p></section><section className="auth-card"><form onSubmit={submit}><h2>{signup?'ĐĂNG KÝ':'ĐĂNG NHẬP'}</h2>{signup&&<label><User/><input name="name" placeholder="Họ và tên" required/></label>}<label><Mail/><input name="email" type="email" placeholder="Email" required/></label><label><LockKeyhole/><input name="password" type="password" minLength={8} placeholder="Mật khẩu" required/></label>{message&&<p className="form-message">{message}</p>}<button disabled={busy}>{busy?'Đang xử lý…':signup?'Đăng ký':'Đăng nhập'}</button><div className="or">hoặc</div><button className="secondary" type="button" onClick={()=>{setSignup(!signup);setMessage('')}}>{signup?'Quay lại đăng nhập':'Tạo tài khoản client'}</button></form></section></main>}
