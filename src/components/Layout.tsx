import{BarChart3,FileText,LayoutDashboard,LogOut,Menu,Settings,Trash2,Users,X}from'lucide-react';import{NavLink}from'react-router-dom';import{useState,type ReactNode}from'react';import{useAuth}from'../contexts/AuthContext';import{NotificationBell}from'./NotificationBell';import{ThemeSwitch}from'./ThemeSwitch';
const nav=[['/','Tổng quan',LayoutDashboard],['/documents','Hồ sơ',FileText],['/trash','Thùng rác',Trash2],['/statistics','Thống kê',BarChart3],['/settings','Cài đặt',Settings]] as const;

export function Layout({children,theme,onToggleTheme}:{children:ReactNode;theme:'light'|'dark';onToggleTheme:()=>void}){const{profile,signOut}=useAuth(),[open,setOpen]=useState(false);return <div className="shell"><aside className={open?'sidebar open':'sidebar'}><div className="sidebar-title"><strong>HỆ THỐNG QUẢN LÝ</strong><span>Đại học Lạc Hồng</span><button className="mobile-close" onClick={()=>setOpen(false)}><X/></button></div><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}><Icon/>{label}</NavLink>)}{profile?.role==='admin'&&<NavLink to="/users" onClick={()=>setOpen(false)}><Users/>Quản lý người dùng</NavLink>}</nav></aside><div className="app"><header><button className="menu-button" onClick={()=>setOpen(true)}><Menu/></button><b>ĐẠI HỌC LẠC HỒNG</b><div className="header-actions"><ThemeSwitch checked={theme==='dark'} onChange={onToggleTheme} className="header-theme-switch"/><NotificationBell/><span className="user-chip">{profile?.full_name||profile?.email}<small>{profile?.role}</small></span><button title="Đăng xuất" onClick={signOut}><LogOut/></button></div></header><main className="content">{children}</main><footer>© 2026 Lac Hong University · Hệ thống quản lý hồ sơ</footer></div></div>}
