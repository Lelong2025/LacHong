import{BarChart3,FileText,LayoutDashboard,LogOut,Menu,Settings,Trash2,Users,X}from'lucide-react';import{NavLink}from'react-router-dom';import{useState,type ReactNode}from'react';import{useAuth}from'../contexts/AuthContext';import{NotificationBell}from'./NotificationBell';import{ThemeSwitch}from'./ThemeSwitch';
const clientNav=[['/','Tổng quan',LayoutDashboard],['/documents','Hồ sơ',FileText],['/statistics','Thống kê',BarChart3],['/trash','Thùng rác',Trash2],['/settings','Cài đặt',Settings]] as const;
const adminNav=[['/','Tổng quan',LayoutDashboard],['/documents','Hồ sơ',FileText],['/statistics','Thống kê',BarChart3],['/users','Quản lý người dùng',Users],['/trash','Thùng rác',Trash2],['/settings','Cài đặt',Settings]] as const;

export function Layout({children,theme,onToggleTheme}:{children:ReactNode;theme:'light'|'dark';onToggleTheme:()=>void}){const{profile,signOut}=useAuth(),[open,setOpen]=useState(false);const nav=profile?.role==='admin'?adminNav:clientNav;return <div className="shell"><aside className={open?'sidebar open':'sidebar'}><div className="sidebar-title"><strong>HỆ THỐNG QUẢN LÝ</strong><span>Trung Tâm Nghiên Cứu Khoa Học & Ứng Dụng</span><button className="mobile-close" onClick={()=>setOpen(false)}><X/></button></div><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}><Icon/>{label}</NavLink>)}</nav></aside><div className="app"><header><button className="menu-button" onClick={()=>setOpen(true)}><Menu/></button><b className="header-title"><span className="header-title-full">TRUNG TÂM NGHIÊN CỨU KHOA HỌC & ỨNG DỤNG</span><span className="header-title-short">TTNCKH&UD</span></b><div className="header-actions"><ThemeSwitch checked={theme==='dark'} onChange={onToggleTheme} className="header-theme-switch"/><NotificationBell/><span className="user-chip">{profile?.full_name||profile?.email}<small>{profile?.role}</small></span><button title="Đăng xuất" onClick={signOut}><LogOut/></button></div></header><main className="content">{children}</main><footer>© 2026 Trung Tâm Nghiên Cứu Khoa Học & Ứng Dụng · Hệ thống quản lý hồ sơ</footer></div></div>}
